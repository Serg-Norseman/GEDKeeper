using System;

namespace GKCore
{
	/*public sealed class WeightedMean<T>
	{
		private T result;
		private T wsum;

		public WeightedMean()
		{
			this.result = default(T);
			this.wsum = default(T);
		}
		
		public void AddValue(T value, T weight)
		{
			this.result += (value * weight);
			this.wsum += weight;
		}
		
		public T GetResult()
		{
			if (wsum != 0.0f) {
				return result / wsum;
			} else {
				return default(T);
			}
		}
	}*/
}
