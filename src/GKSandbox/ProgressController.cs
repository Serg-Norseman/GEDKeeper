
using System;
using System.Threading;

namespace GKSandbox
{
	/*public delegate void DoSomeWorker(object status);

	public class ProgressController
	{
		public ProgressController()
		{
		}

		public static void Run(DoSomeWorker worker)
		{
			TfmProgress progress = new TfmProgress();
			ThreadPool.QueueUserWorkItem(new System.Threading.WaitCallback(worker), progress);
			progress.ShowDialog();
		}
	}*/

	/*public void DoCheckGEDCOMFormat(object status)
	{
		IProgressController prCtl = status as IProgressController;
		TreeTools.CheckGEDCOMFormat(this.FTree, this.fValuesCollection, prCtl);
	}*/

	//ProgressController.Run(DoCheckGEDCOMFormat);
	//this.DoCheckGEDCOMFormat(this);
}
